{
  "Logging": {
    "LogLevel": {
      "Default": "Error"
    }
  },
  "JWTSettings": {
    "TokenKey": "This is a secret key and needs to be at least 12 characters",
    "Issuer": "localhost",
    "Audience": "localhost",
    "EncryptKey": "16CharEncryptKey"
  },
  "AllowedHosts": "*"
}
